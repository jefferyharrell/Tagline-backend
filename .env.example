# Tagline Backend Example Environment Variables

# --- Database ---
# Database connection string for development (default)
DATABASE_URL=postgresql://tagline:tagline@postgres:5432/tagline
# Database connection string for integration tests (clean DB, wiped before every test run)
TEST_DATABASE_URL=postgresql://tagline:tagline@localhost:5432/tagline_test
# For production:
# DATABASE_URL=postgresql://<user>:<password>@<host>:5432/<database>

# --- Application Environment ---
# Set to 'production' or 'development'.
APP_ENV=production

# --- Storage Provider ---
# Options: filesystem, memory, null, dropbox
STORAGE_PROVIDER=memory
# FILESYSTEM_STORAGE_PATH=/tmp  # Required if STORAGE_PROVIDER=filesystem
# DROPBOX_APP_KEY=your-dropbox-app-key
# DROPBOX_APP_SECRET=your-dropbox-app-secret
# DROPBOX_REFRESH_TOKEN=your-dropbox-refresh-token
# DROPBOX_ROOT_PATH=/your/dropbox/files

# --- Authentication ---
# API key for authenticating backend requests (required for production)
TAGLINE_API_KEY=your-very-secret-api-key

# --- Logging ---
LOG_LEVEL=INFO

# Comma-separated list of allowed CORS origins (for browser-based frontends)
# Example: http://localhost:3000,https://myfrontend.com
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Log Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# Determines the verbosity of application logs.
# Defaults to INFO if not set or invalid.
# LOG_LEVEL=INFO

# Maximum in-memory thumbnail cache size (in MB) for /photos/{id}/thumbnail endpoint
# Default: 100
THUMBNAIL_CACHE_MAX_MB=100

# Maximum in-memory image cache size (in MB) for full image requests
# Default: 200
IMAGE_CACHE_MAX_MB=200
