# REQUIRED: Database connection string for all environments
# For local dev/test (in-memory SQLite):
# DATABASE_URL=sqlite:///:memory:
# For local dev (file-based SQLite):
# DATABASE_URL=sqlite:///./tagline.db
# For production (Postgres example):
# DATABASE_URL=postgresql://user:password@localhost:5432/tagline

# Thumbnail cache size (MB)
THUMBNAIL_CACHE_MAX_MB=100
# Image cache size (MB)
IMAGE_CACHE_MAX_MB=200
# ...
